{% block sw_landing_page_detail_cms_form %}
    <div class="lae-language-inheritance--cms-page-wrapper">
        <div
                class="lae-language-inheritance--cms-page-active-notice"
                v-if="landingPage?.customFields?.laeLanguageInheritance"
        >
            <p class="outline">
                {{ $tc('lae-language-inheritance.sw-landing-page.inheritance-active') }}
            </p>
            <mt-button
                    @click="openPageLanguageInheritanceModal = true
"
            >
                <sw-icon
                        small
                        name="regular-globe-stand"
                />
                {{ $tc('lae-language-inheritance.sw-cms-page-form.button') }}
            </mt-button>
        </div>
        <div
                :class="{'lae-language-inheritance--cms-page-active': landingPage?.customFields?.laeLanguageInheritance}"
        >
            {% parent %}
        </div>
    </div>

    <lae-language-inheritance-modal
            v-if="openPageLanguageInheritanceModal"
            :initLanguage="landingPage?.customFields?.laeLanguageInheritance"
            @select-language="onSelectLanguage"
            @modal-close="openPageLanguageInheritanceModal = null"
    />
{% endblock %}